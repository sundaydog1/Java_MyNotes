package day13_CustomMethod;

public class ReturnStatement {
/*
    public static void main(String[] args) {
        eligible(25);

       int num = 5;
       if (num>3){
    return; //  if its true and thisblock gets executed ,it will terminate the main method in this case


    }

    public static void eligible(int age) {
        if (age < 0 || age > 150) {
            System.err.println("Invalid age:" + age);
            return; //exits the method. because the methods' return type is VOID, so no value can be returned
            // if the age is invalid, then this method gets terminated, and everything after this line will
            // be terminated as well
        }

        if (age >= 21) {
            System.out.println("You are eligible to buy alcohol");
        } else {
            System.out.println("You are not eligible");
        }


    }
public static int multiplication (int n1, int n2){
        int result = n1*n2;
        return result; // returns a result

}




*/
}